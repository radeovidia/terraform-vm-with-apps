version: '3.8'
services:
  backend:
    build: ./backend
    container_name: company_be

  frontend:
    build: ./frontend
    container_name: company_fe
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
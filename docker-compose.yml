version: "3.9"

services:

  backend:
    build: ./backend
    container_name: go-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: react-frontend
    ports:
      - "80:80"